import bees from './getBees.js';

const beesChanged = bees().map(bee => bee + ' in ocean')



export { beesChanged as oceanicBees }

export const cats = "We have no cats in oceans";

export const isThereBeesInOceann = true;

export default "The Ocean Kingdom";